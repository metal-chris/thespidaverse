import { AnalyticsDashboard } from "./AnalyticsDashboard";

export const metadata = {
  title: "Analytics — The Spidaverse",
  robots: "noindex, nofollow",
};

export default function AnalyticsPage() {
  return <AnalyticsDashboard />;
}
